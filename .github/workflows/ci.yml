name: CI

on:
  pull_request:
    branches: [testing, staging, main]

jobs:
  backend-tests:
    name: Backend (pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest tests/ -v --tb=short --cov=app --cov-report=term-missing

  frontend-tests:
    name: Frontend (vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          rm -f package-lock.json
          npm install

      - name: Run tests
        env:
          VITE_SUPABASE_URL: https://omhfdzcrusahvvzoljpf.supabase.co
          VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9taGZkemNydXNhaHZ2em9sanBmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI5MTAzNzgsImV4cCI6MjA4ODQ4NjM3OH0.Gs2DRBBoY3BJlE8w6VuC2GT5vWfXsPMImT1X0Zg9m6M
        run: npm test -- --run
